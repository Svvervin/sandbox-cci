version: 2.1

  
jobs:
  my-job:
    docker:
    - image: cimg/base:2024.01
      auth:
        username: fake-user-123  # can specify string literal values
        password: password123  # or project environment variable reference
    resource_class: small
    
    steps:
      - checkout

workflows:
  le-workfw5:
    jobs:
      - my-job

