version: 2.1

jobs:
  build: # name of your job
    resource_class: 'windows.medium'
    machine:
      image: 'windows-server-2022-gui:current'
      shell: 'powershell.exe -ExecutionPolicy Bypass'
    steps:
      # Commands are run in a Windows virtual machine environment
        - checkout
        - run: Write-Host 'Hello, Windows'
       # - run: setx $CIRCLE_WORKINNG_DIRECTORY "$($CIRCLE_WORKING_DIRECTORY | 's/project//')" /M 
        - run:
            command: sshdir=${CIRCLE_WORKING_DIRECTORY::-8}
            shell: bash.exe
        - run:
            command: echo $sshdir
            shell: bash.exe
        - run:
            shell: bash.exe
            command: |
            cd ${CIRCLE_WORKING_DIRECTORY::-8}
            ls -al
            
