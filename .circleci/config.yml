version: 2.1

orbs:
  macos: circleci/macos@2.4.0
  
jobs:
  build:
    macos:
      xcode: 15.1.0
    resource_class: macos.x86.medium.gen2
    
    steps:
      - run: xcodebuild -version
      - run: brew --version
      - run: whoami
      - run: pip --version
      
      
workflows:
  say-hello-workflow:
    jobs:
      - build
