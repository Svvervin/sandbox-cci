version: 2.1

jobs:
  who-me:
    docker:
      - image: google/cloud-sdk:359.0.0
    steps:
      - run: apt install jq -y
      - run: jq --version

workflows:
  docker-who:
    jobs:
      - who-me
