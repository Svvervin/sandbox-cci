version: 2.1

orbs:
  macos: circleci/macos@2.4.0
  
jobs:
  build:
    macos:
      xcode: 15.2.0
    resource_class: macos.m2.medium.gen2
    
    steps:
      - run: xcodebuild -version
      - run: brew --version
      - run: whoami
      - run: 'sudo chown -R distiller: /opt'
      
      
workflows:
  say-hello-workflow:
    jobs:
      - build
