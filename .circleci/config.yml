version: 2.1

orbs:
  macos: circleci/macos@2.4.0
  
jobs:
  build:
    macos:
      xcode: 14.3.1
    resource_class: macos.m1.medium.gen1
    
    steps:
      - run: xcodebuild -version
      - run: brew --version
      - macos/enable-vnc
    
      
workflows:
  say-hello-workflow:
    jobs:
      - build
