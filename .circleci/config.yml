version: 2.1
    
jobs:
  my-job:
    docker:
    - image: cimg/openjdk:8.0
    resource_class: small
    
    steps:
      - checkout          
      - run: circleci version
      - run: circleci config validate

workflows:
  le-workfw5:
    jobs:
      - my-job

