version: 2.1

jobs:
  build:
    machine:
      image: ubuntu-2204:2024.01.1
    steps:
      - checkout
      - run: pwd
      - run: sudo apt install tree
      - run: tree
      - run: echo "hi"
      - run:
          name: get token
          command: echo 'export ROLLBAR_ACCESS_TOKEN=$(yq -r ".ROLLBAR_ACCESS_TOKEN" /home/circleci/project/config/env.yaml)' >> $BASH_ENV
      - run: echo $ROLLBAR_ACCESS_TOKEN

workflows:
  workflow1:
    jobs:
      - build
 
