version: 2.1

jobs:
  update:
    parameters:
      img:
        type: string
    docker:
      - image: << parameters.img >>
        user: circleci
    steps:
     
      # - run: sed -i 's/stable\/updates/stable-security\/updates/' /etc/apt/sources.list
      # - run: echo "deb http://archive.debian.org/debian stretch main" | sudo tee -a /etc/apt/sources.list
      - run: sudo apt-get update

workflows:
  aptgetupdate:
    jobs:
      - update:
          matrix:
            parameters:
              img:
                - notion/circleci:0.157
            
