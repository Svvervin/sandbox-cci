version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:18.13.0
    steps:
      - checkout
      - run:
          name: get token
          command: echo 'export ROLLBAR_ACCESS_TOKEN=$(yq -r ".ROLLBAR_ACCESS_TOKEN" /config/env.yaml)' >> $BASH_ENV
      - run: echo $ROLLBAR_ACCESS_TOKEN

workflows:
  workflow1:
    jobs:
      - build
 
