version: 2.1

jobs:
  machine-job:
    machine:
      image: default
    steps:
      - checkout 
      - run: echo "hello world"
      - run: pwd

workflows:
  my-workflow:
    jobs:
      - machine-job
