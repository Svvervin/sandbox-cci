version: 2.1

jobs:
  hello-job:
    docker:
      - image: ubuntu-2004:202107-02 # the primary container, where your job's commands are run
       
    steps:
      - checkout # check out the code in the project directory
      - run: echo "hello world 1" # run the `echo` command

workflows:
  my-workflow:
    jobs:
      - hello-job
