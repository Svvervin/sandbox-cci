version: 2.1

jobs:
  update:
    parameters:
      img:
        type: string
    docker:
      - image: << parameters.img >>
        user: circleci
    steps:
     
      # - run: sed -i 's/stable\/updates/stable-security\/updates/' /etc/apt/sources.list
      # - run: echo "deb http://archive.debian.org/debian stretch main" | sudo tee -a /etc/apt/sources.list
      - run: sudo apt-get update

workflows:
  aptgetupdate:
    jobs:
      - update:
          matrix:
            parameters:
              img:
                - notion/circleci@sha256:fa1d8345d43c6b7b413d88a8205797b376eeca32c4fe4e7ecff41aaeb5b3f17a
            
