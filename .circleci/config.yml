version: 2.1

jobs:
  who-me:
    machine:
      image: ubuntu-2204:2022.10.1
    steps:
      - run: docker run singledigital/awx-ee:4.x whoami
      - run: timeout 2m ping -w5 google.com

workflows:
  docker-who:
    jobs:
      - who-me
