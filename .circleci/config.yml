version: 2.1

jobs:
  machine-job:
    machine:
      image: ubuntu-2204:2023.10.1
    steps:
      - checkout 
      - run: echo "hello world"
      - run: pwd

workflows:
  my-workflow:
    jobs:
      - machine-job
