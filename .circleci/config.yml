version: 2.1
    
jobs:
  my-job:
    docker:
    - image: cimg/openjdk:21.0.0
    resource_class: small
    
    steps:
      - checkout          
      - run: |
          ssh -V
  my-job-2:
    docker:
    - image: cimg/base:stable
    resource_class: small
    
    steps:
      - checkout          
      - run: |
          ssh -V

workflows:
  le-workfw5:
    jobs:
      - my-job
      - my-job-2
