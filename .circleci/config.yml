version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:current
    resource_class: runner-egrapp/k8srunner
    steps:
      - run: echo "Hi I'm on a Container Runner! "
      - run: ping google.com
workflows:
  build-workflow:
    jobs:
      - build
