version: 2.1

orbs:
  cli: circleci/circleci-cli@0.1.9

executors:
    default:
        docker:
            - image: circleci/circleci-cli:0.1.23845

jobs:
  orb-publish:
    executor: default
    
    steps:
      - checkout
      - run: circleci version
      
workflows:
  main:
    jobs:
      - orb-publish
