version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:current
    resource_class: runner-egrapp/k8srunner
    steps:
      - run: echo "Hi I'm on a Container Runner! is there task step testing"
      - run:
          command: |
            while true; do
              sleep 50
              echo "this in running in the background"
            done
          background: true
workflows:
  build-workflow:
    jobs:
      - build
