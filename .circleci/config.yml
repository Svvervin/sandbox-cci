version: 2.1
    
jobs:
  my-job:
    docker:
    - image: cimg/openjdk:8.0
    resource_class: small
    
    steps:
      - checkout          
      - run: ssh -V
      - run: Write-Host 'Hello, this is gonna fail because not Windows'

workflows:
  le-workfw5:
    jobs:
      - my-job

