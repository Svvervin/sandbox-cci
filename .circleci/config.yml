version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:18.13.0
    steps:
      - checkout
      - run: pwd
      - run: echo "hi"
      - run:  
           name: "Fix CIRCLE_WORKING_DIRECTORY"  
           command: echo 'CIRCLE_WORKING_DIRECTORY="${CIRCLE_WORKING_DIRECTORY/#\~/$HOME}"' >> $BASH_ENV
      - run: echo $CIRCLE_WORKING_DIRECTORY
      - run:
          name: get token
          command: echo 'export ROLLBAR_ACCESS_TOKEN=$(yq -r ".ROLLBAR_ACCESS_TOKEN" $CIRCLE_WORKING_DIRECTORY/config/env.yaml)' >> $BASH_ENV
      - run: echo $ROLLBAR_ACCESS_TOKEN

workflows:
  workflow1:
    jobs:
      - build
 
