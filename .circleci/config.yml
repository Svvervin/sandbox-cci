version: 2.1


jobs:
  say-hello:
      docker:
      - image: cimg/postgres:16.0.0-postgis
      resource_class: medium+
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo Hello, World!"
      - run: sudo apt install postgresql-15-pgvector

 
workflows:
  say-hello-workflow:
    jobs:
      - say-hello
     
