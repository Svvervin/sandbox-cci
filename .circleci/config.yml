version: 2.1

jobs:
  hello-job:
    docker:
      - image: cimg/postgres:15.2-postgis # the primary container, where your job's commands are run
    steps:
      - checkout # check out the code in the project directory
      - run: echo "hello world" # run the `echo` command
      - run: sudo apt install postgresql-15-pgvector

workflows:
  my-workflow:
    jobs:
      - hello-job
