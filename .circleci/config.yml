
version: 2.1


jobs:
  say-hello:
    docker:
      - image: cimg/base:current
    steps:
        - checkout
        - run: circleci version
        - run: circleci orb list --host cciserver.logangodsey.com --token $CIRCLE_TOKEN

workflows:
  say-hello-workflow:
    jobs:
      - say-hello

