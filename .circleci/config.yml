version: 2.1

jobs:
  who-me:
    docker:
      - image: google/cloud-sdk:359.0.0
    steps:
      - run: sudo apt-get install jq
      - run: jq --version

workflows:
  docker-who:
    jobs:
      - who-me
