version: 2.1

jobs:
  hello-job:
    docker:
      - image: circleci/python:3.9-buster # the primary container, where your job's commands are run
       
    steps:
      - checkout # check out the code in the project directory
      - run: echo "hello world 1" # run the `echo` command

workflows:
  my-workflow:
    jobs:
      - hello-job
