version: 2.1

orbs:
  cli: circleci/circleci-cli@0.1.9

jobs:
  orb-publish:
    executor: cli/default
    steps:
      - checkout
      - cli/install
      - run: circleci version
      
 workflows:
  main:
    jobs:
      - orb-publish
